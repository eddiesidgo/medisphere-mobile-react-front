import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'medisphere-mobile-react-front',
  webDir: 'dist'
};

export default config;
